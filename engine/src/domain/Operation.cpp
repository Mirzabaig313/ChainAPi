// Operation value-type implementations live in the header; this TU exists
// to provide a link-time anchor for the domain library and a place for
// future helper functions (e.g. method-name parsing).
#include <chainapi/engine/Operation.h>

namespace chainapi::engine {

// Intentionally empty. See the header.

}  // namespace chainapi::engine
